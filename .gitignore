# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
# Note: package-lock.json should be committed (we use npm)
# yarn.lock and pnpm-lock.yaml are ignored if accidentally created
yarn.lock
pnpm-lock.yaml

# testing
/coverage
*.test.js.snap
.nyc_output

# next.js
/.next/
/out/
next-env.d.ts

# production
/build
/dist

# debug logs
npm-debug.log*
# Ignore other package manager logs if accidentally used
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# local env files (contains secrets - never commit)
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Environment files (uncomment if you want to ignore these too)
# .env.development
# .env.production
# .env.test

# vercel
.vercel

# typescript
*.tsbuildinfo
tsconfig.tsbuildinfo

# prisma
/prisma/migrations
# Prisma client is generated in node_modules, which is already ignored above

# IDE and editor files
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.iml
*.sublime-project
*.sublime-workspace

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Temporary files
*.tmp
*.temp
*.swp
*.swo
*~
.cache/
.turbo/

# Server logs directory
server/logs/

# Security keys
*.pem
*.key
*.cert
